<div>
  <section class="container wrapper">

    <div class="col-lg-12">

      <!-- TITLE -->
      <div class="row m-b">
        <div class="col-lg-12">
          <h1 class="txt-green inline">{{ 'add_an_event' | translate }}</h1>
        </div>
      </div>

      <form role="form" name="eventForm" novalidate action="{{ actionUrl }}" ng-upload="submited(content)" upload-options-enable-rails-csrf="true">
        <ng-include src="'<%= asset_path 'events/_form.html' %>'"></ng-include>
      </form>

    </div>

  </section>

</div>


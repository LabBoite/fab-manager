<div>
  <form role="form" name="projectForm" novalidate action="{{ actionUrl }}" ng-upload="submited(content)" upload-options-enable-rails-csrf="true" unsaved-warning-form>
    <section class="container wrapper">

      <div class="col-lg-12">

        <!-- TITLE -->
        <div class="row m-b">
          <div class="col-lg-12">
            <h1 class="txt-green">{{ 'edit_the_project' | translate }} <span class="badge" ng-if="project.state == 'draft'" translate>{{ 'rough_draft' }}</span></h1>
          </div>
        </div>

        <ng-include src="'<%= asset_path 'projects/_form.html' %>'"></ng-include>

      </div>

    </section>
  </form>
</div>
